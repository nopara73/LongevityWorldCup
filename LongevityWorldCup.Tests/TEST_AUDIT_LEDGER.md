# Test audit ledger

## September 2026 source snapshot pruning

The requested suite reduction removes 321 test cases (224 fact/theory methods). Against the 1,599-case master suite, this is a 20.08% reduction to 1,278 cases, including the recently added URL-compatibility and athlete-contact browser cases.

The removed tests predominantly searched HTML, CSS, and JavaScript for exact source fragments, helper names, markup, colors, or copy. They constrained implementation details without executing the behavior named by the test. The motion-source auditor and tests of its own parser were removed together. This intentionally reduces source-shape coverage; it does not claim that every deleted assertion has an equivalent behavioral assertion.

| Removed snapshots | Retained coverage |
| --- | --- |
| Application, review, proof-upload, profile-edit, and selected-athlete page snapshots | `NewAthleteOnboardingBrowserTests`, `ProofUploadBrowserTests`, `PlayAthleteFlowBrowserTests`, controller validation, payment policy, submission retry, and image-processing tests; retain the original source checks for the proof cap, duplicates, unsupported/mixed selections, and per-file recovery |
| Stored-biomarker and calculator handoff snapshots | Calculator/API tests, `BioageFlowBrowserTests`, `BioageMobileUxBrowserTests`, keyboard/viewport tests, and rank-preview tests; retain all six original automatic-unit-correction/handoff checks |
| Play menu, shared flow controls, CSS motion, accessibility markup, badge colors, radar drawing, and leaderboard script snapshots | Every browser test, including the flow, accessibility, responsive-layout, reduced-motion, proof-viewer, and navigation scenarios; ranking, badge, and Event tests |
| Aesthetic-system and statistics source/copy snapshots | Served-page routing, versioned assets, local font files, standalone-page isolation, statistics service, dashboard browser, and tracking browser tests |
| Exact utility-page titles and ruleset diagram copy/placement | Keep every indexing-policy assertion from the title tests in the existing canonical-page response checks, which already request those same pages |

The original best-rank candidate-set and modal-metadata checks also remain because the route browser tests do not exercise those contracts. Source checks with unique ranking, unit-conversion, or proof-processing coverage are retained even when a future behavioral replacement would be preferable.

Surviving assertions are unchanged; indexing-policy assertions move into the existing canonical-page tests. `ApplicationSubmissionTimeout_WaitsForDedicatedServerTimeout` moves intact into `PublicRequestTimeoutTests` so the browser/server deadline relationship stays covered. No test is skipped, filtered out of CI, or converted into a passing placeholder.

## Earlier architecture audit

The counts and measurements below are historical results from the earlier architecture work.

The sub-minute runtime target was abandoned in favor of correctness-first repair. Performance changes below are retained only where the protected contract remains explicit and repeated runs show a real saving. No test was removed merely for being slow.

## Coverage disposition

| Test or surface | Problem found | Final action | Evidence and retained/replacement coverage | Runtime saved |
| --- | --- | --- | --- | --- |
| `AestheticSystemBrowserTests` | One very large class serialized layout, content, responsive-media, and accessibility contracts that do not share scenario state. | Split into independently scheduled contract classes while retaining common assertion helpers. | Every original fact/theory row and assertion remains. Each test still owns a physical browser context; the classes run only through the four bounded browser lanes. | Included in the browser critical-path saving |
| `FlowActionDockBrowserTests` | A multi-thousand-line class serialized form, layout, and result-state contracts. Its generic placement test also repeated the same shared dock algorithm over all 70 route/viewport combinations. | Split into independently scheduled form, layout, result, and core classes. Rewrite the generic audit to 30 representative combinations and execute at most two isolated scenarios concurrently. | Every route is audited at constrained mobile and desktop anchors; `/join` and `/apply?fake=1` cover all five additional viewport shapes. The original audit script is unchanged, and focused route-specific responsive tests remain. Each combination still gets a fresh physical context. | Generic matrix: 36.59 s to 12.95 s in loaded full runs; 16.0 s to 8.41 s focused after bounded execution |
| `GuessMyAgeBrowserTests` | Celebration, reduced-motion, persistence/re-entry, and core submission behavior were serialized together. The core class also started a fresh Kestrel host for every test even though its requests are intercepted or deliberately rejected without changing Crowd Age. | Split by observable contract and use the lane application for the non-mutating core class. | Every test retains its own browser context and storage. The one real endpoint case asserts the rejected guess does not change the count; accepted behavior remains covered by controller tests and the browser cases' representative responses. | Removes roughly 8-10 s of hidden per-test host lifecycle work |
| `HomepageChromeRegressionBrowserTests` | Header, sticky action, contribution, responsive action, and play-action behavior were serialized and repeated page setup. | Split by contract and merge the four equivalent global-play-action route permutations into one bounded case. | All four routes, six viewport shapes, top/sticky/scrolled phases, geometry, and action assertions remain. Route pages are independent and the shared context contains no mutable scenario data. | Global play-action case measured about 2.6-3.9 s focused/loaded |
| Browser fixture architecture | Per-test Chromium and Kestrel ownership dominated runtime; aggressive physical-context reuse later proved unsafe for clocks, input, history, viewport, and storage. | Share one ref-counted Chromium process across four measured lanes, one application per lane, and retain a fresh physical browser context per test. | The accepted design never virtualizes browser state. Stateful scenarios use distinct data, and each context receives a deterministic documentation-only client address for the analytics request so the real per-client limiter is exercised without treating all simulated users as one loopback client. The dedicated Kestrel regression still proves 240 admissions followed by a real 429. | Browser-named subset: 249 clean tests in 1:49 test time / 1:54.4 wall |
| Browser collection order | xUnit's default collection order changed after a rebuild, delaying browser lanes A/B from about 3 seconds to 29-34 seconds and turning a clean 2:11 run into a clean 3:12 run. | Prioritize the four existing browser collections, then order all remaining collections deterministically by display name. | The orderer does not change xUnit's machine-aware worker limit, collection boundaries, fixtures, or assertions. Tests remain order-independent; only long critical-path lanes are submitted first. | Removes up to 31 s of observed scheduling delay and stabilizes run time |
| Read-only HTTP contract classes (35 classes) | Each class booted an identical full application for GET/HEAD/OPTIONS and service-read contracts. | Merge them into one explicit read-only HTTP collection fixture. | All 35 classes were inspected for writes. The one Crowd Age metadata case that seeds guesses already creates and owns a separate factory; stateful controller/statistics tests remain outside the collection. | Included in the full-suite saving; removes 34 redundant host starts |
| Browser Bitcoin dependencies | Browser hosts called live public providers, while local `/api/bitcoin/*` interception bypassed our controller and service in UI tests. | Replace only outbound provider HTTP with a deterministic fail-closed factory and remove every first-party Bitcoin interceptor. | `BrowserTestAppTests.BitcoinEndpointsUseRealApplicationBehaviorWithDeterministicProviders` executes the real controller/service/cache path; `UnconfiguredExternalHostsFailClosedWithoutNetworkAccess` proves unknown hosts cannot open a network dependency. | Not claimed |
| `BrowserTestAppTests.ParallelStartsUseUniqueKestrelAssignedPorts` | The speed branch reduced eight concurrent starts to two. | Restore all eight concurrent starts. | The original concurrency/load witness and unique-port assertions are intact. | 0 s |
| `ApplicationControllerValidationTests` | 107 class-serialized cases created 33 identical application factories. | Reuse one class fixture. | Assertions and cases are unchanged; tests use delta-relative or unique submission data and remain serialized within the class. | About 68 s aggregate to 2 s test time (5.4 s wall) |
| Social image rendering tests | Thirteen serialized cases repeatedly booted the same service graph. An earlier speed attempt incorrectly avoided some real renders. | Reuse one class fixture but retain every production render and PNG decode/dimension assertion. | Athlete, league, page, chronological-age, improvement, Crowd Age, domain-winner, X, custom-event, and meme image paths all remain covered. The single Crowd write and badge refresh do not invalidate any other case's relative assertions. | 54.23 s aggregate to 12.23 s focused |
| Event cleanup and Guess controller tests | Eleven serialized cases repeatedly created identical hosts. | Reuse one fixture within each class. | Every stateful Event case begins by clearing and reseeding `Events`; Guess assertions compare before/after deltas, so the one accepted guess cannot invalidate another case. | Combined focused aggregate: 31.04 s to 4.03 s |
| Site-statistics service tests | A privacy assertion searched the entire serialized dashboard for `1980`; unrelated timestamp fractional digits could contain that sequence. | Rewrite the assertion around the observable persisted contract. | The event route and landing route must equal the canonical query-free route, and metadata must contain exactly the expected `entryMode`. Per-test databases remain unchanged. | Flake removed; no speed claim |
| Browser resource routing | Every browser request passed through a catch-all route handler even though only four known third-party providers require deterministic substitution. | Route only analytics and known third-party URLs; install the catch-all observer only for tests that explicitly need it. | Unknown external hosts still fail closed at the application HTTP boundary, and browser boundary tests execute the real first-party Bitcoin endpoints. | 20.918 s focused saving |
| `StaticFileFormattingTests.PublicHtmlAndInjectedPartialsEndWithNewline` | Enforced only an incidental trailing-newline convention and exercised no application behavior. | Remove. | `.editorconfig` already declares `insert_final_newline = true` for HTML. A missing final LF cannot change the rendered response or expose an application regression. No replacement test is needed. | 0.004 s recorded test time; no wall-clock claim |
| `FrontendTypeScriptBuildTests.EveryReusableJavascriptAssetHasMatchingTypeScriptSource` | Exactly duplicated the repository-owned frontend output verifier. | Remove. | `npm run build` deletes `wwwroot/js`, runs TypeScript, and invokes `Scripts/verify-frontend-outputs.mjs`, which rejects nested runtime sources plus missing or unexpected JavaScript. CI runs that command before the .NET build and tests. | 0.015 s recorded test time; no wall-clock claim |
| `AestheticSystemPageTests.GuessMyAge_RestoresBoundedRevealChoreographyWithoutHeavyDependencies` | A 100-plus-assertion source-string snapshot coupled the suite to exact function names, constants, statements, and CSS text instead of an observable contract. | Remove. | The Guess browser suite executes the endpoint/troll path, ordinary and exact reveals, reduced-motion transitions, persistence ordering, resource caps, viewport bounds, cancellation, cleanup, re-entry, and renderer failures in Chromium. Those tests fail on the user-visible or bounded-work regression without prescribing its implementation. | 4.026 s recorded in the baseline run, including shared-fixture startup that transfers to the next test; no wall-clock claim |
| `MotionRestraintPageTests.BioAgeResultVisualReveal_IsBoundedSharedAndReducedMotionAware` | Asserted exact private constants, function calls, keyframe names, and CSS declarations already exercised through the browser. | Remove. | `BioageFlowBrowserTests.BioageResultReveal_KeepsTheSemanticResultImmediateAndHonorsReducedMotion` covers both calculators with and without reduced motion, including semantic immediacy, viewport visibility, stage ordering, animation duration, bounded visual updates, settled state, and cleanup. Re-entry cancellation remains separately covered. | 0.071 s recorded test time; no wall-clock claim |
| Repository-wide assertion/duplication/flake scan | Candidate scan across all 996 remaining fact/theory declarations. | Retain the remaining suite. | No assertion-free or permanently skipped tests, exact duplicate method bodies, duplicate inline-data rows, mocking-framework tautologies, snapshots, or blanket retries were found. The only remaining `Task.Delay` is 10 ms inside a bounded asynchronous polling helper; browser fixed sleeps remain absent. | 0 s |
| `LongevitymaxxingChallengeBrowserTests.ReplyPagingUpdatesOnlyDiscussionSurfacesAtTheRequestedCursor` | A late, uncontrolled athlete-directory response could rebuild the page during the scenario, intermittently merging two differently-cursored reply surfaces. | Rewrite and retain; fix the product invariant. | The athlete request is now held behind an explicit gate and released after one surface is paged. The test observes successful avatar hydration and confirms that auxiliary data updates do not rebuild or corrupt either discussion surface. Production now hydrates only the affected avatar nodes. | Flake removed; no speed claim |
| `GuessSubmission_StorageFailureStillRevealsAndAllowsEveryExit` | It replaced `Storage.prototype.getItem`/`setItem`, coupling the test to a monkey-patched implementation and making restoration part of correctness. | Rewrite. | The test exhausts the real Chromium origin quota, verifies the browser's quota exception, and retains the submit, reveal, persistence-failure, timer, close, and skip assertions. | Not claimed |
| xUnit worker configuration | Hard-coded worker counts tied the suite to one machine and both five browser lanes and a seven-slot runner configuration regressed locally. | Remove/retain machine defaults. | xUnit chooses its machine-aware limit. The orderer changes submission order only; the only concurrency bounds are the four measured browser lanes and degree two inside the isolated read-only placement audit. | Rejected five-lane run: 2:14; rejected seven-slot run: 2:14 |

## Added or rewritten regression protection

| Test | Problem | Action and evidence | Runtime saved |
| --- | --- | --- | --- |
| `PublicPostRateLimitingTests.AnalyticsRateLimitPreservesTooManyRequestsStatusOverKestrel` | No real-server test covered status-code re-execution after limiter rejection. | Add. It proves 240 admitted analytics posts followed by 429 plus positive `Retry-After`. | Coverage added |
| `CrowdAgeProfileImageTests.SourceReloadPublishesCrowdStatsAsOneCompleteSnapshot` | No test sampled public snapshots during a real source reload. | Add. It uses the public watcher/event boundary and continuously asserts Crowd Age/count consistency. | Coverage added |
| `BrowserTestAppTests.BitcoinEndpointsUseRealApplicationBehaviorWithDeterministicProviders` | UI tests either depended on live providers or stubbed the first-party endpoint. | Add. It calls all three first-party Bitcoin endpoints and proves the external provider requests remain behind the deterministic boundary. | Coverage added |
| `BrowserTestAppTests.UnconfiguredExternalHostsFailClosedWithoutNetworkAccess` | An accidental new outbound dependency could silently make the suite network-dependent again. | Add. An unknown HTTPS host returns an in-process 503 and is recorded without opening a socket. | Coverage added |
| `BrowserTestAppTests.SynchronousFactoryDisposalRemovesItsWorkingDirectory` and `AsyncFactoryDisposalRemovesItsWorkingDirectory` | Deferred host disposal could return before the entry-point thread finished disposing SQLite, best-effort cleanup hid the leak, and the first deterministic repair covered only asynchronous callers. A later review also found that a disposal timeout could bypass deletion and that startup cleanup could replace the original failure. | Add both lifecycle contracts. Each starts the real app service graph, disposes it through one public ownership mode, and requires the exact per-test workspace to be gone. Cleanup is attempted after either success or failure; simultaneous lifecycle/cleanup failures are aggregated, and browser startup preserves its original exception alongside any cleanup exception. | Coverage added |
| `BrowserTestAppTests.CustomDatabaseFactoryDisposalWaitsForTheServiceProvider` | The disposal sentinel was activated only by the default `DatabaseManager` factory. Tests that replaced that service could bypass the wait and race cleanup against SQLite disposal. | Add synchronous and asynchronous custom-database cases. The sentinel is now the test host lifetime, resolved while the host is built and independent of replaceable application services; each case proves both the factory workspace and externally owned database can be deleted immediately after disposal. | Coverage added |
| `DrainableOperationLifetimeTests.StopWaitsForEveryAdmittedOperationAndRejectsNewOnes` | Joining the watcher worker did not cover another request or database callback already waiting on or owning the athlete mutation semaphore when shutdown began. The first flat drain then rejected the synchronous badge refresh descended from an admitted mutation. | Add a focused lifecycle regression. The shared primitive closes general admission atomically, admits and counts descendants of a still-active logical operation, rejects unrelated work, waits for the complete admitted operation tree, and tolerates idempotent lease disposal; `AthleteDataService` uses it at every reload-gated entry point before disposing synchronization state. | Coverage added |
| `BioageResultReveal_KeepsTheSemanticResultImmediateAndHonorsReducedMotion` | Real-time animation and final CSS-frame sampling were load-sensitive. | Rewrite synchronization with a pre-navigation clock, deterministic advancement, animation completion, and exact settled-opacity checks; assertions remain strict. | Not claimed |
| `BioageMobileUxBrowserTests.RestoredDraftEdit_InvalidatesThePreviouslyCalculatedHandoff` | The waiter could be registered after navigation and waited for unrelated full-load resources. | Arm navigation observation around the action and assert the destination document/URL contract. | Not claimed |
| `FlowActionDockBrowserTests.ApplyNextStageTransition_DoesNotForceViewportScroll` | Playwright could auto-scroll a control before the dock had settled. | Wait for the action's visible settled state, perform the real click, cross the layout boundary, and retain the no-scroll assertions. | Not claimed |
| `FlowActionDockBrowserTests.MobileBioageStickyProgress_KeepsOnlyVisibleProgressSemantics` | Programmatic scroll and class readiness did not prove real input dispatch or final accessibility presentation. | Use real wheel input, verify actual scroll movement, and wait for every asserted opacity/pointer/inert/ARIA property. | Not claimed |
| `GuessSubmission_UsesExistingDismissalWithoutAddingAResultAction` and `GuessSubmission_StorageFailureStillRevealsAndAllowsEveryExit` | Paused-clock callbacks and close transitions could deadlock or be sampled early. | Install the clock before navigation, advance unchanged product timers explicitly, and wait for the mutation/transition boundary. | Not claimed |
| `GuessSubmission_AnimatesThroughTheRevealAndBoundsCelebrationWork` | The test entered `guess-mode` and immediately wrote and read the range before the production `MutationObserver` reset and focused it. Under mixed-suite load, either side of that race could win. | Wait for the complete observable post-transition contract before interacting: current athlete and image, guess mode active, result not ready, range enabled at its default value and focused, and no stale result bubble. The existing reveal, reaction, animation, and work-bound assertions remain strict. | Not claimed |
| `ProofViewer_HistoryClosesOneLayerAtATime` | Sequential reads could miss a transient complete modal state. | Capture the visible age text atomically with URL, athlete, and loading state. | Not claimed |
| Delayed proof-helper tests | Fixed network sleeps did not prove the helper request was pending. | Replace sleeps with request-start/release gates while retaining pending and recovery assertions. | Not claimed |
| `SlowAndOfflineEventRequests_ShowLoadingAndRecoveryStates` | Its request-start gate used `SetResult` and assumed exactly one matching `/api/events` request; a second request could throw inside the route handler. A later review found that a failed start waiter could also bypass the explicit release and leave context teardown waiting on the held route. | Make the gate idempotent and force a second matching request while the first remains held. Both requests must reach the gate and return 200 after one explicit release, and a `finally` always releases the route before asynchronous context disposal. | Not claimed |
| `ResultUpload_BoundsLargeNoisyPdfCanvasBeforeKeepingIt` | Random compressed output made the precondition probabilistic. | Use deterministic real encoding and assert both the oversized first encode and bounded accepted encode. | Not claimed |
| Challenge Gravatar background tests | `SpinWait` and unsynchronized lists made results CPU- and ordering-sensitive. | Use `ConcurrentQueue<T>` plus bounded async polling; retain request, fallback, cache, and priority assertions. | Not claimed |

## Count and assertion checks

- Current master baseline: 1,484 tests.
- Final suite: 1,489 tests; the nine additions are the Kestrel 429 regression, atomic athlete-reload regression, real Bitcoin endpoint boundary regression, deny-by-default external HTTP regression, the synchronous and asynchronous default factory-disposal regressions, the synchronous and asynchronous custom-database factory-disposal regressions, and the drainable-operation lifetime regression, while four low-value or redundant tests were removed after individual coverage review.
- Fact/theory declarations: 992 baseline, 996 final.
- Inline/member/class data declarations: 625 baseline, 627 final.
- Assertion-call sites: 8,346 baseline, 8,313 final. The four audit removals eliminated 152 assertion call sites from the pre-audit PR head, 148 of them from the two incidental source-string snapshots; the custom-database disposal regression then added three executable lifecycle assertions.
- Final test removals: four, individually documented above.

## Final verification

- Pre-repair no-coverage baseline: 1,491 passed, 0 failed, 0 skipped in 10 minutes 9.947 seconds wall clock.
- Application-controller fixture check: 107 passed, 0 failed, 0 skipped in 2 seconds test time (5.376 seconds wall clock), down from about 68 seconds of aggregate class time.
- Social-image, Event-cleanup, and Guess-controller fixture check: 26 passed, 0 failed, 0 skipped in 12 seconds test time (14.441 seconds wall clock), down from 85.27 to 16.26 seconds of aggregate test time. Every image-render assertion remained enabled.
- Browser repetition check: the 30-case placement matrix and seven core Guess cases passed 8 of 8 in 12 seconds test time (17.406 seconds wall clock); the placement audit itself fell from 16.0 to 8.41 seconds focused.
- Best no-coverage checkpoint for the accepted architecture: 1,491 passed, 0 failed, 0 skipped in 2 minutes 11 seconds test time (2 minutes 15.653 seconds wall clock), a 7 minute 54.294 second / 77.8% wall-clock reduction from the same-command baseline.
- Rejected experiments were not retained: eight browser lanes took 2 minutes 36 seconds, five lanes took 2 minutes 14 seconds, four Chromium processes took 2 minutes 19 seconds, seven xUnit slots took 2 minutes 14 seconds, and browser-context pooling took 2 minutes 20 seconds. The accepted architecture keeps four lanes, one Chromium process, machine-default xUnit scheduling, and physical context isolation.
- Untouched master comparison under the same coverage instrumentation: 1,483 passed, 1 failed, 0 skipped in 16 minutes 21 seconds test time. Coverlet did not emit a baseline report because the run failed.
- The Guess reveal regression initially passed 10 of 10 in isolation but failed on run 4 of a mixed 18-test workload, proving a suite-context timing race. After waiting for its exact MutationObserver postcondition, the same mixed workload passed 10 of 10 fresh-process runs: 180 of 180 test executions in 9 minutes 41.9 seconds.
- Affected real Bitcoin/UI boundary tests: 6 passed, 0 failed, 0 skipped.
- Initial pull-request CI after the semantic fixture rewrite: 1,444 passed and 44 failed, all in `FlowActionDockBrowserTests`, because the class-shared application exhausted its real analytics rate limiter.
- Complete Flow class after correcting the analytics client-isolation boundary: 92 passed, 0 failed, 0 skipped in 4 minutes 13 seconds test time (4 minutes 49 seconds wall clock), including all 44 CI failures. The later shared-lane design retains distinct client identities and has the dedicated same-client limiter regression.
- Complete instrumented suite after the CI repair and network-boundary cleanup: 1,488 passed, 0 failed, 0 skipped in 10 minutes 39 seconds test time (10 minutes 50.9 seconds wall clock).
- Complete instrumented suite after the browser and athlete-service lifecycle repairs, before the synchronous factory regression was added: 1,489 passed, 0 failed, 0 skipped in 12 minutes 20.5 seconds test time (12 minutes 30.8 seconds wall clock), with 73.76% line, 57.85% branch, and 82.99% method coverage.
- Complete instrumented suite before the final review fixes: 1,490 passed, 0 failed, 0 skipped in 12 minutes 36 seconds test time (12 minutes 48.7 seconds wall clock). The temp-directory count returned from one active workspace to the same 7,725 baseline after teardown.
- Post-review complete instrumented suite: 1,490 passed, 0 failed, 0 skipped in 11 minutes 48 seconds test time (12 minutes 1.1 seconds wall clock). The temp-directory count again returned to the exact 7,725 baseline.
- Earlier detailed coverage checkpoint: 73.80% line, 57.93% branch, 83.04% method. No baseline coverage percentage is claimed because the baseline run failed before report generation.
- The asynchronous teardown regression passed 10 of 10 fresh processes without creating a workspace. A later mixed run exposed that ordinary synchronous `using` still lacked the same cleanup boundary. After repairing both public disposal modes, the pair passed 20 of 20 executions across 10 fresh processes and the temp-directory count remained 7,725.
- Affected lifecycle and browser batch: 30 passed, 0 failed, 0 skipped, with no additional temp directories.
- Post-review duplicate-request, reload, and synchronous/asynchronous teardown set: 10 of 10 fresh-process runs passed, 40 of 40 affected executions; the temp-directory count remained 7,725.
- Final operation-tree shutdown batch: 10 of 10 fresh-process runs passed, 110 of 110 affected executions; the temp-directory count remained 7,733 before and after.
- Earlier correctness checkpoint before the final performance repair: the complete instrumented suite passed 1,491 tests with 0 failed and 0 skipped in 12 minutes 1 second test time (12 minutes 13.5 seconds wall clock). The temp-directory count was unchanged at 7,733 before and after.
- Current-head GitHub Actions verification: all 1,491 tests passed with 0 failed and 0 skipped under coverage. The test command completed in 3 minutes 33.56 seconds; current coverage is 73.8% line and 58.0% branch. The complete restore, build, Node-free publish check, Playwright installation, test, and coverage job completed in 5 minutes 21 seconds.
- Three consecutive current-head local full-suite runs passed 1,491 of 1,491 with 0 skipped each. Their TRX elapsed times were 2 minutes 56.527 seconds, 3 minutes 8.689 seconds, and 4 minutes 30.599 seconds. The host slowed under uninterrupted back-to-back CPU saturation, but no `testhost` or Playwright Chromium process remained after the sequence; the independent coverage-instrumented GitHub run completed in 3 minutes 33.56 seconds.
- After the final held-route teardown review fix, `SlowAndOfflineEventRequests_ShowLoadingAndRecoveryStates` passed with a clean affected-project rebuild (0 warnings, 0 errors), and repository formatting verification passed for every file changed by this PR.
- Final audit-removal batch: 64 affected frontend tests passed with 0 failed and 0 skipped after a clean build. The first complete run then exposed the late athlete-directory hydration defect: 1,486 passed, 1 failed, and 0 skipped in 3 minutes 48 seconds test time (233.164 seconds wall clock).
- Athlete-hydration repair: the deterministic reply-paging regression passed 10 of 10 fresh-process runs, and the complete Challenge browser class passed 14 of 14 with 0 skipped.
- Final post-audit complete suite: 1,487 passed, 0 failed, and 0 skipped in 2 minutes 24 seconds test time (149.374 seconds wall clock). No project was excluded, no retry was added, and the normal machine-default scheduling and browser isolation remained intact.
- Exact pre-review tree after the formatter cleanup: all 83 Challenge service tests passed after a 0-warning build, then all 1,487 tests passed with 0 failed and 0 skipped in 2 minutes 32 seconds test time (157.105 seconds wall clock). Solution formatting verification also passed.
- Review lifecycle batch: the two custom-database disposal paths passed 10 of 10 fresh processes (20 of 20 executions), then the complete `BrowserTestAppTests` and `CrowdAgeProfileImageTests` set passed 12 of 12. The 22 pre-existing Crowd Age workspace count remained exactly 22 throughout.
- Post-review full suite: 1,489 passed, 0 failed, and 0 skipped. A run overlapping another workspace's `dotnet build .\Flux.slnx` took 3 minutes 16 seconds test time (210.469 seconds wall clock); the identical already-built binaries then passed without that contention in 2 minutes 56 seconds test time (183.220 seconds process wall, including about 7 seconds of `dotnet test` startup and teardown).
- The 7,725 historical pre-fix workspaces were not deleted as part of this audit. Eight additional workspaces came only from explicitly interrupting a local run after discovering that its requested collector was unavailable; their exact paths were identified, no test process remained, and recursive removal was blocked by the execution policy. Successful runs before and after that interruption leaked none.
